{
  "name": "crud",
  "version": "1.0.0",
  "description": "News Api, in this we have news collection and users collection with this we have to do crud and we have to show that which user has did all the crud after updating data in news and articles",
  "main": "api.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "omkar",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.26.0",
    "bcrypt": "^5.0.1",
    "body-parser": "^1.19.2",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "jsonwebtoken": "^8.5.1",
    "mongodb": "^4.4.1",
    "nodemon": "^2.0.15",
    "router": "^1.3.6"
  }
}
